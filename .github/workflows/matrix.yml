name: Matrix
on: push

jobs:
  node-version:
    runs-on: ubuntu-latest
    steps:
      - name: Log Node Version
        run: node -v
      - uses: actions/setup-node@v3
        with:
          node-version: 16
      - name: Log Node Version
        run: node -v
